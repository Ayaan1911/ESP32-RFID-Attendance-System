# Frontend

React + Vite + Tailwind dashboard for live attendance viewing.

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

The dashboard reads from `http://localhost:8000/attendance` and keeps the API key only in React state for the current session.
